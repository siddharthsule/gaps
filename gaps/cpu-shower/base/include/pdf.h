#ifndef pdf_h_
#define pdf_h_

/**
 * Not needed! LHAPDF is already a header-only library. A wrapper for LHAPDF is
 * needed only for GPU, where we need to do evaluation for all flavours and then
 * select the relevant one. in C++ you state the flavours you want from the
 * beginning.
 */

#endif  // pdf_h_